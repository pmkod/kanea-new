import { BlockedIpModel } from "../models/blocked-ip-model";

export const checkIfIpIsAuthorized = async () => {
  //   const blockedIp = await BlockedIpModel.findOne({ ip: c.ip, cannotDoAnyAction: true });
  //   if (blockedIp !== null) {
  //     throw Error("Error");
  //   }
  //   await next();
};
